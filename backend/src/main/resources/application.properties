# 1. Database Connection URL
# Use the Render host URL. Note: jdbc:postgresql prefix is mandatory for Java.
spring.datasource.url=jdbc:postgresql://dpg-d5nj6cbe5dus73f6h0r0-a.frankfurt-postgres.render.com/fpl_db_sriv

# 2. Separate Credentials
# Use environment variables (explained below) for better security!
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}

# 3. Automatic Configuration (Modern cleanup)
# Spring Boot 3+ automatically detects the Driver and Dialect from the URL.
# You can safely remove the hardcoded lines to keep the file clean.

# 4. Persistence Settings (CRITICAL)
# Change from 'create-drop' to 'update' so your data isn't deleted on restart.
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.main.allow-circular-references=true
fpl.league.id=152